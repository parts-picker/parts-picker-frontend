import { H1 } from "@blueprintjs/core";
import { FC } from "react";

export const dynamic = "force-dynamic";

const Home: FC = () => {
  return (
    <div>
      <main>
        <H1>parts picker</H1>
      </main>
    </div>
  );
};

export default Home;
