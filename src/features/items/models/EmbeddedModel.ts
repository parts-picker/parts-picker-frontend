/**
 * Classes derived from this interface are expecteded to contain
 * exactly one attribute of type array of a class derived from ResponseModel.
 * The name of the attribute can be chosen freely.
 */
// eslint-disable-next-line
export default interface EmbeddedModel<Content> {}
